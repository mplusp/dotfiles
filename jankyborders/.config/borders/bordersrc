#!/usr/bin/env bash

THEME_DIR="$XDG_CONFIG_HOME/borders/themes"
source "$THEME_DIR/tokyonight"

options=(
	active_color="$ACTIVE_COLOR"
	inactive_color="$INACTIVE_COLOR"
	style="$STYLE"
	width="$WIDTH"
	hidpi="$HIDPI"
)

borders "${options[@]}"
